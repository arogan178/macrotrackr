You are a triage subagent with full codebase access and the desloppify CLI.
Your job is to complete the **SENSE-CHECK** stage of triage planning.

Repo root: /home/arogan178/Documents/repos/GitHub/app/macro_tracker

## Standards

You are expected to produce **exceptional** work. The output of this triage becomes the
actual plan that an executor follows — if you are lazy, vague, or sloppy, real work gets
wasted. Concretely:

- **Read the actual source code.** Every opinion you form must come from reading the file,
  not from reading the issue title. Issues frequently exaggerate, miscount, or describe
  code that has already been fixed. Trust nothing until you verify it.
- **Have specific opinions.** "This seems like it could be an issue" is worthless. "This is
  a false positive because line 47 already uses the pattern the issue suggests" is useful.
- **Do the hard thinking.** If two issues seem related, figure out WHY. If something should
  be skipped, explain the specific reason for THIS issue, not a generic category.
- **Don't take shortcuts.** Reading 5 files and extrapolating to 30 is lazy. Read all 30.
  If you have too many, use subagents to parallelize — don't skip.
- The prompt below already contains the authoritative stage contract and prior reports.
  Do NOT search old triage run artifacts for alternate instructions unless you hit a
  concrete mismatch you need to explain.

Use the desloppify CLI to record your work. Every command you run mutates plan.json directly.
The orchestrator will review your work and confirm the stage after you record it.
Use the exact CLI command prefix shown in the CLI Command Reference: `desloppify`.
Do NOT debug, repair, reinstall, or inspect the CLI/environment. If the command fails, stop
and explain the failure in your stdout summary.

After you finish recording the stage, write a short plain-text summary to stdout describing
what you changed, which commands you ran, and any blockers you hit. The runner requires
non-empty stdout output.

**CRITICAL: Only run commands for YOUR stage (SENSE-CHECK).** Do NOT re-run earlier stages
(e.g., do not run `--stage observe` if you are the organize subagent). Earlier stages
are already confirmed. Re-running them will corrupt the plan state.


## Prior Stage Reports


### STRATEGIZE Report
{
  "computed_at": "2026-04-14T00:00:00Z",
  "lookback_scans": 207,
  "focus_dimensions": [
    {
      "name": "Test health",
      "reason": "Highest available headroom, currently stagnant.",
      "headroom": 74.5,
      "trend": "stagnant"
    },
    {
      "name": "Dep health",
      "reason": "High headroom, currently stagnant.",
      "headroom": 65.0,
      "trend": "stagnant"
    },
    {
      "name": "Mid elegance",
      "reason": "High headroom, currently stagnant.",
      "headroom": 35.0,
      "trend": "stagnant"
    },
    {
      "name": "Type safety",
      "reason": "High headroom, currently stagnant.",
      "headroom": 35.0,
      "trend": "stagnant"
    }
  ],
  "avoid_areas": [
    {
      "name": "AI generated debt",
      "reason": "High rework loops indicating whack-a-mole fixes rather than structural solutions.",
      "type": "dimension"
    },
    {
      "name": "contract_coherence",
      "reason": "High rework loops.",
      "type": "dimension"
    },
    {
      "name": "test_strategy",
      "reason": "High rework loops.",
      "type": "dimension"
    }
  ],
  "rework_warnings": [
    {
      "dimension": "ai_generated_debt",
      "resolved": 35,
      "new_open": 3,
      "files": ["."]
    },
    {
      "dimension": "contract_coherence",
      "resolved": 27,
      "new_open": 2,
      "files": ["."]
    },
    {
      "dimension": "test_strategy",
      "resolved": 25,
      "new_open": 3,
      "files": ["."]
    },
    {
      "dimension": "abstraction_fitness",
      "resolved": 21,
      "new_open": 2,
      "files": ["."]
    },
    {
      "dimension": "dependency_health",
      "resolved": 25,
      "new_open": 4,
      "files": ["."]
    }
  ],
  "file_churn_hotspots": [
    {
      "file": "backend/scripts/migrate-dev-users-to-clerk.ts",
      "count": 13,
      "detectors": ["boilerplate_duplication", "orphaned", "smells", "stale_wontfix", "test_coverage"]
    },
    {
      "file": "frontend/src/features/auth/components/ClerkSignInForm.tsx",
      "count": 10,
      "detectors": ["boilerplate_duplication", "orphaned", "smells", "test_coverage"]
    },
    {
      "file": "backend/src/config.ts",
      "count": 8,
      "detectors": ["smells", "stale_wontfix", "subjective_review", "test_coverage"]
    }
  ],
  "stagnant_dimensions": [
    "Test health",
    "Dep health",
    "AI generated debt",
    "Mid elegance",
    "Type safety",
    "API coherence",
    "Error consistency",
    "Structure nav",
    "Cross-module arch"
  ],
  "debt_trend": "stable",
  "score_trend": "stable",
  "momentum_dimensions": [],
  "executive_summary": "The codebase is exhibiting stable but stagnant health metrics, hovering near its all-time high of 79.8. However, this stability masks significant rework loops, particularly in AI generated debt, contract coherence, and test strategy, where high numbers of issues are resolved only to have new ones quickly reopened. There are major opportunities for improvement in neglected high-headroom dimensions such as Test Health (74.5 headroom) and Dependency Health (65.0 headroom). The team must break out of the cycle of treating symptoms in high-churn areas and instead focus on structural improvements in these neglected, high-impact domains.",
  "observe_guidance": "Focus verification efforts on uncovering root causes of the high reopen rates in AI generated debt and contract coherence. Look beyond surface-level fixes to identify systemic patterns causing regressions. Pay special attention to file churn hotspots like backend/scripts/migrate-dev-users-to-clerk.ts and frontend/src/features/auth/components/ClerkSignInForm.tsx, as they indicate areas of instability.",
  "reflect_guidance": "Reflect on the high levels of rework and churn. Stop treating AI generated debt as a whack-a-mole game; analyze why the generators keep producing it. Shift focus toward structurally improving Test Health and Dependency Health, which offer the largest potential gains but remain stagnant. Avoid further minor tweaks in highly volatile dimensions.",
  "organize_guidance": "Prioritize structural fixes in Test Health and Dependency Health. Deprioritize or batch low-value fixes in high-churn dimensions (AI generated debt, contract coherence) unless they address root causes. Order work to resolve foundational issues first before tackling surface-level symptoms. Ensure file churn hotspots receive targeted, comprehensive refactoring rather than incremental patches.",
  "sense_check_guidance": "Are we confusing activity with progress? The high resolution rates in certain dimensions are negated by high reopen rates. Question the value of fixes that do not address the underlying systemic issues. Focus on interventions that yield lasting improvements in stagnant, high-headroom areas.",
  "anti_patterns": [
    {
      "type": "repeated refactoring of the same area",
      "description": "High resolution and reopen counts in specific dimensions.",
      "evidence": "AI generated debt (35 resolved, 27 reopens), contract coherence (27 resolved, 23 reopens)."
    },
    {
      "type": "neglected high-headroom dimensions",
      "description": "Dimensions with large headroom remaining stagnant.",
      "evidence": "Test Health (74.5 headroom) and Dep health (65.0 headroom) are stagnant."
    },
    {
      "type": "score-neutral churn",
      "description": "High execution activity that does not meaningfully move the overall strict score.",
      "evidence": "Stable score trend (79.1 to 79.5) despite 144 total resolved issues."
    }
  ],
  "strategic_issues": [
    {
      "identifier": "address-test-health-neglect",
      "summary": "Test health is severely neglected with a headroom of 74.5 and stagnant trend.",
      "priority": "critical",
      "recommendation": "Shift major focus to improving test coverage and addressing test health issues structurally.",
      "dimensions_affected": [
        "Test health"
      ]
    },
    {
      "identifier": "address-dependency-health",
      "summary": "Dependency health has a large headroom (65.0) but remains unaddressed.",
      "priority": "high",
      "recommendation": "Perform a comprehensive audit and cleanup of project dependencies, addressing duplicated lockfiles and redundant packages.",
      "dimensions_affected": [
        "Dep health"
      ]
    },
    {
      "identifier": "investigate-ai-debt-rework",
      "summary": "AI generated debt has a very high rework loop (35 resolved, 27 reopens, 3 new open).",
      "priority": "high",
      "recommendation": "Identify the root cause of recurring AI generated debt. Is there a flawed prompt, template, or automated process continually introducing these issues?",
      "dimensions_affected": [
        "AI generated debt"
      ]
    }
  ]
}


### ORGANIZE Report
Organized 6 manual clusters according to the reflect blueprint:
- backend-auth-cleanup
- backend-lib-refactor
- api-contract-standardization
- test-strategy-revamp
- dependency-and-architecture
- misc-code-health

Promoted auto-cluster auto/stale_exclude. Added specific action steps for each cluster and ordered them logically. test-strategy-revamp depends on api-contract-standardization to ensure we don't write tests for things about to be changed.


### ENRICH Report
Enriched all clusters by adding detailed executor-ready steps. Every step now has 80+ chars of detail including specific file paths, explicit issue references, and effort tags. Addressed all 34 open review issues across the 6 manual clusters.


## Strategic Flags
Are we confusing activity with progress? The high resolution rates in certain dimensions are negated by high reopen rates. Question the value of fixes that do not address the underlying systemic issues. Focus on interventions that yield lasting improvements in stagnant, high-headroom areas.
- Rework loop: ai_generated_debt (35 resolved, 3 new open)
- Rework loop: contract_coherence (27 resolved, 2 new open)
- Rework loop: test_strategy (25 resolved, 3 new open)
- Rework loop: abstraction_fitness (21 resolved, 2 new open)
- Rework loop: dependency_health (25 resolved, 4 new open)
- High resolution and reopen counts in specific dimensions.
- Dimensions with large headroom remaining stagnant.
- High execution activity that does not meaningfully move the overall strict score.

## Issue Summary
Open review issues: 34
Open dimensions: abstraction_fitness (2), ai_generated_debt (3), api_surface_coherence (2), authorization_consistency (1), contract_coherence (2), cross_module_architecture (1), dependency_health (4), design_coherence (1), error_consistency (2), high_level_elegance (2), incomplete_migration (1), initialization_coupling (1), logic_clarity (1), low_level_elegance (2), mid_level_elegance (2), package_organization (1), test_strategy (3), type_safety (3)
## Available Investigation Commands

To see full detail for a specific issue:
  desloppify show <issue-id-or-hash-pattern> --no-budget
To see all open review issues with suggestions:
  desloppify show review --status open --no-budget
To see a cluster's steps, members, and suggestions:
  desloppify plan cluster show <name>
To list all clusters:
  desloppify plan cluster list --verbose

## SENSE-CHECK Stage Instructions

This stage is handled by three subagents. If you are being run as a
single-subprocess fallback, perform ALL three checks below.

Execution order: content batches (parallel) → structure batch → value batch (sequential).
Value runs last because it needs the corrected plan state from content and structure.

### Content Check (per cluster)
Use `desloppify show <issue-hash> --no-budget` to verify step details match the original reviewer analysis.

For EVERY step in every cluster, read the actual source file and verify:
1. OVER-ENGINEERING (check FIRST — if the fix fails this, skip the rest):
   Would this change make the codebase *worse*? Flag steps that:
   - Add abstractions, wrappers, or indirection for a one-time operation
   - Introduce unnecessary config/feature-flags/generalization
   - Make simple code harder to read for marginal benefit
   - Gold-plate beyond what the issue actually requires
   - Trade one smell for a worse one (e.g. fix duplication by adding a fragile base class)
   - Add more lines of code than they remove (net complexity increase)
   Also flag steps that:
   - Do not extract code into new files or functions that would have exactly 1 consumer
   - Do not use __internal or _test export hacks — test through the public API or export properly
   - Do not rename for convention alone when no ambiguity exists
   - Do not delete tests without equivalent replacement coverage
   - Do not strip rationale comments — preserve comments explaining why, not what
   - Refactors must preserve behavior — do not change test expectations in cleanup steps
   - Net line count must decrease or stay flat in cleanup commits
   Remove or simplify over-engineered steps. If the whole cluster is net-negative,
   recommend removing it entirely — don't just flag it, say "skip this cluster."
2. STALENESS: Is the problem still present, or already fixed?
3. LINE NUMBERS: Does the code at the claimed lines match the step description?
4. NAMES: Do function/variable/type names in the step exist in the file?
5. COUNTS: Are counts ("update the 3 imports") accurate?
6. VAGUENESS: Could a developer with zero context execute this step?
7. EFFORT TAGS: Does the tag match actual scope?
8. DUPLICATES: Flag steps that duplicate work in another cluster.

Fix with: `desloppify plan cluster update <name> --update-step N --detail "..." --effort <tag>`

### Structure Check (global)
Build a file-touch graph and check:
1. SHARED FILES: Two clusters touching same file without --depends-on → add dependency
2. MISSING CASCADE: Rename/remove without importer updates → add cascade step
3. CIRCULAR DEPS: Flag cycles, don't add them

Fix with: `desloppify plan cluster update <name> --depends-on <other>`
Fix with: `desloppify plan cluster update <name> --add-step "..." --detail "..." --effort trivial --issue-refs <hash>`


### Value Check (global — YAGNI/KISS pass)

Walk the CURRENT planned work item by item and make the final judgment about value.
Ask: does doing this make the codebase genuinely better? Beauty is a valid reason to keep work,
but not if it buys that beauty with new indirection, wrappers, abstraction layers, or confusion.

For EVERY live queue target, choose exactly one:
1. `keep` — clearly improves correctness, clarity, cohesion, simplicity, or elegance
2. `tighten` — worth doing, but the plan must be simplified or made more concrete first
3. `skip` — the fix would add churn, indirection, coordination, or abstraction for too little gain

What should usually be skipped:
- facade pruning that just spreads imports
- abstraction-for-abstraction's-sake
- tiny theoretical cleanups that make the code harder to follow
- fixes whose implementation is more complicated than the current code

What can still be worth keeping:
- simplifications that delete layers or reduce branching
- aesthetic cleanups that genuinely improve readability without adding machinery
- focused unifications that make naming or flow more coherent with less confusion

Use these commands aggressively:
- `desloppify next --count 100` to inspect the current execution queue
- `desloppify plan cluster show <name>` to inspect cluster members and steps
- `desloppify show <issue-id-or-hash> --no-budget` to re-read the underlying finding
- `desloppify plan cluster update <name> --description "..." --update-step N --detail "..." --effort small`
- `desloppify plan skip --permanent <pattern> --note "<why>" --attest "I have reviewed this triage skip against the code and I am not gaming the score by suppressing a real defect."`
- `desloppify plan cluster delete <name>` if you skipped everything it contained


Required process:
1. Re-read the actual code behind each queue target.
2. Apply the rubric above, not the raw issue title.
3. Tighten any keeper whose steps are too vague, too broad, or too complicated.
4. Permanently skip anything that fails the value test.
5. Delete dead clusters after skipping all their members.

Required report structure — include a Decision Ledger section:
```
## Decision Ledger
- cluster-or-id -> keep
- cluster-or-id -> tighten
- cluster-or-id -> skip
```

When done, run:
```
desloppify plan triage --stage sense-check --report "<findings summary>"
```



## CLI Command Reference

### Stage recording
```
desloppify plan triage --stage strategize --report "<json briefing>"
desloppify plan triage --stage observe --report "<analysis>"
desloppify plan triage --stage reflect --report "<strategy>" --attestation "<80+ chars>"
desloppify plan triage --stage organize --report "<summary>" --attestation "<80+ chars>"
desloppify plan triage --stage enrich --report "<enrichment summary>" --attestation "<80+ chars>"
desloppify plan triage --stage sense-check --report "<verification summary>" --attestation "<80+ chars>"
```

### Cluster management
```
desloppify plan cluster create <name> --description "<what this cluster addresses>"
desloppify plan cluster add <name> <issue-patterns...>
desloppify plan cluster remove <name> <issue-patterns...>
desloppify plan cluster update <name> --description "<desc>" --steps "step 1" "step 2"
desloppify plan cluster update <name> --add-step "<title>" --detail "<sub-points>" --effort small --issue-refs <id1> <id2>
desloppify plan cluster update <name> --update-step N --detail "<sub-points>" --effort medium --issue-refs <id1>
desloppify plan cluster update <name> --depends-on <other-cluster-name>
desloppify plan cluster delete <name>
desloppify plan cluster show <name>
desloppify plan cluster list --verbose
desloppify plan promote <pattern>
desloppify plan promote <pattern> top
desloppify plan promote <pattern> before -t <target>
```

### Skip/dismiss
```
desloppify plan skip --permanent <pattern> --note "<reason>" --attest "I have reviewed this triage skip against the code and I am not gaming the score by suppressing a real defect."
```
**Important:** The --attest value MUST contain the words "reviewed" and "not gaming" (exact phrases required by validation).
Every review issue in the active triage run must end up either in a manual cluster or permanently skipped. Leaving issues unassigned is an invalid triage result.

### Issue investigation
```
desloppify show <issue-id-or-pattern> --no-budget    # full issue detail with suggestion
desloppify show review --status open --no-budget     # all open review issues
desloppify backlog --count 20                        # inspect backlog-only detector work
```

### Effort tags
Valid values: trivial, small, medium, large. Set on steps via --effort flag.


## Validation Requirements (ALL BLOCKING)
- Re-runs ALL enrich-level checks (detail, issue_refs, effort, paths, vagueness)
- Stage must be recorded with a 100+ char report
- Report must include a `## Decision Ledger` with one line per live queue target
- Every live queue target must appear exactly once as keep, tighten, or skip
- Report must cite real file paths to prove the code was re-read
- Stage must be confirmed with an 80+ char attestation mentioning cluster names or clearly describing the verified sense-check work

