import { describe, expect,it } from "vitest";

import { reportingApi } from "./reporting";

describe("reportingApi", () => {
  it("should be defined", () => {
    expect(reportingApi).toBeDefined();
  });
});
