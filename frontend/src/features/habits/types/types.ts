/**
 * Re-export habit types from shared location for backwards compatibility
 * New code should import from @/types/habit
 */
export type {
  HabitAccentColor,
  HabitCardProps,
  HabitGoal,
  HabitGoalFormValues,
  HabitsState,
  HabitTrackerProps,
} from "@/types/habit";
