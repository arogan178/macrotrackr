import { describe, expect,it } from "vitest";

import * as hooks from "./useAuthQueries"; 

describe("useAuthQueries", () => {
  it("should be defined", () => {
    expect(hooks).toBeDefined();
  });
});
